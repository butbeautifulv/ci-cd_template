# Optional local vendor cache (xlsx/pdf for re-extract only; not required at runtime)
.external/*
# Symlink to cxado-references (created by cxado `make refs-link`)
refs/
# Symlinks to cxado-skills (created by cxado `make skills-link`)
.agents/skills/
# Symlinks to cxado-agent-rules (created by cxado `make rules-link`)
.agents/rules/
.codebase-memory/
