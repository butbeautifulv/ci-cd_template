name: Container Scan

on:
  workflow_call:
    inputs:
      image:
        required: true
        type: string

jobs:
  trivy-image:
    runs-on: ubuntu-latest
    steps:
      - uses: aquasecurity/trivy-action@0.28.0
        with:
          image-ref: ${{ inputs.image }}
          severity: CRITICAL,HIGH
          format: sarif
          output: trivy-image.sarif
          exit-code: "0"
      - name: Gate check
        run: python scripts/gate-check.py --control container --report trivy-image.sarif
      - uses: github/codeql-action/upload-sarif@v3
        if: always()
        with:
          sarif_file: trivy-image.sarif
          category: container
