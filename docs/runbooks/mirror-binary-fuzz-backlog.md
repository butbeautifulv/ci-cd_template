# Binary fuzz backlog (T6)

Deferred until Schemathesis (T5) is green on corp mirror.

| Track | Notes |
|-------|--------|
| AFL++ / libFuzzer | Existing Fabrica binary-fuzz jobs — Nexus image pin required |
| Go native fuzz | `go test -fuzz` in service modules when present |
| Jazzer | JVM services only |

Out of this wave: no mirror profile include, no DD control wiring for binary fuzz yet.
