# JCSF extract: CIS Docker

Source: `JCSF v7_public.xlsx` sheet `CIS Docker`.

| ID в CIS Docker | Требование | Примечание | JCSF |
| --- | --- | --- | --- |
| Host Configuration |  |  |  |
| Linux Host Specific Configuration |  |  |  |
| 1.1.1 | Ensure a separate partition for containers has been created | All Docker containers and their data and metadata is stored under /var/lib/dockerdirectory. By default, /var/lib/docker should be mounted under either the / or /varpartitions dependent on how the Linux operating system in use is configured. | DOCK-1-2 |
| 1.1.2 | Ensure only trusted users are allowed to control Docker daemon | The Docker daemon currently requires access to the Docker socket which is, by default, owned by the user root and the group docker. | DOCK-3-2 |
| 1.1.3 | Ensure auditing is configured for the Docker daemon | Audit all Docker daemon activities. | ORCH-2-8; DOCK-2-1 |
| 1.1.4 | Ensure auditing is configured for Docker files and directories - /run/containerd | Audit /run/containerd. | ORCH-2-8; DOCK-2-2 |
| 1.1.5 | Ensure auditing is configured for Docker files and directories - /var/lib/docker | Audit /var/lib/docker. | ORCH-2-8; DOCK-2-2 |
| 1.1.6 | Ensure auditing is configured for Docker files and directories - /etc/docker | Audit /etc/docker. | ORCH-2-8; DOCK-2-2 |
| 1.1.7 | Ensure auditing is configured for Docker files and directories - docker.service | Audit the docker.service if applicable. | ORCH-2-8; DOCK-2-2 |
| 1.1.8 | Ensure auditing is configured for Docker files and directories - containerd.sock | Audit containerd.sock, if applicable. | ORCH-2-8; DOCK-2-2 |
| 1.1.9 | Ensure auditing is configured for Docker files and directories - docker.sock | Audit docker.sock, if applicable. | ORCH-2-8; DOCK-2-2 |
| 1.1.10 | Ensure auditing is configured for Docker files and directories - /etc/default/docker | Audit /etc/default/docker, if applicable. | ORCH-2-8; DOCK-2-2 |
| 1.1.11 | Ensure auditing is configured for Docker files and directories - /etc/docker/daemon.json | Audit /etc/docker/daemon.json, if applicable. | ORCH-2-8; DOCK-2-2 |
| 1.1.12 | Ensure auditing is configured for Docker files and directories - /etc/containerd/config.toml | Audit /etc/containerd/config.toml if applicable. | ORCH-2-8; DOCK-2-2 |
| 1.1.13 | Ensure auditing is configured for Docker files and directories - /etc/sysconfig/docker | Audit /etc/sysconfig/docker if applicable. | ORCH-2-8; DOCK-2-2 |
| 1.1.14 | Ensure auditing is configured for Docker files and directories - /usr/bin/containerd | Audit /usr/bin/containerd if applicable. | ORCH-2-8; DOCK-2-2 |
| 1.1.15 | Ensure auditing is configured for Docker files and directories - /usr/bin/containerd-shim | Audit /usr/bin/containerd-shim if applicable. | ORCH-2-8; DOCK-2-2 |
| 1.1.16 | Ensure auditing is configured for Docker files and directories - /usr/bin/containerd-shim-runc-v1 | Audit /usr/bin/containerd-shim-runc-v1 if applicable. | ORCH-2-8; DOCK-2-2 |
| 1.1.17 | Ensure auditing is configured for Docker files and directories - /usr/bin/containerd-shim-runc-v2 | Audit /usr/bin/containerd-shim-runc-v2 if applicable. | ORCH-2-8; DOCK-2-2 |
| 1.1.18 | Ensure auditing is configured for Docker files and directories - /usr/bin/runc | Audit /usr/bin/runc if applicable. | ORCH-2-8; DOCK-2-2 |
| General Configuration |  |  |  |
| 1.2.1 | Ensure the container host has been Hardened | A container host is able to run one or more containers. It is of utmost importance to harden the host to mitigate host security misconfiguration. | NODES-1-1; NODES-1-2; NODES-1-3; NODES-1-6; NODES-1-7; NODES-2-1; NODES-2-2; NODES-2-3; NODES-2-4; NODES-2-5; NODES-2-7; NODES-2-8; NODES-2-11; NODES-2-12; NODES-3-1; NODES-3-2; NODES-3-3; NODES-3-4; NODES-3-6; NODES-3-7; NODES-4-1; NODES-4-3 |
| 1.2.2 | Ensure that the version of Docker is up to date | Frequent releases for Docker are issued which address security vulnerabilities, resolve product bugs and bring in new functionality. You should keep a tab on these product updates and upgrade as frequently as possible in line with the general IT security policy of your organization. | ORCH-2-2; ORCH-2-7; ORCH-2-12; DOCK-2-6; DOCK-2-9 |
| Docker Daemon Configuration |  |  |  |
| 2.1 | Run the Docker daemon as a non-root user, if possible | Rootless mode executes the Docker daemon and containers inside a user namespace, with both the daemon and the container are running without root privileges. | DOCK-3-7 |
| 2.2 | Ensure network traffic is restricted between containers on the default bridge | By default, all network traffic is allowed between containers on the same host on the default network bridge. If not desired, restrict all inter-container communication. Link specific containers together that require communication. Alternatively, you can create custom network and only join containers that need to communicate to that custom network. | DOCK-4-4 |
| 2.3 | Ensure the logging level is set to 'info' | Set Docker daemon log level to info. | DOCK-1-1 |
| 2.4 | Ensure Docker is allowed to make changes to iptables | The iptables firewall is used to set up, maintain, and inspect the tables of IP packet filter rules within the Linux kernel. The Docker daemon should be allowed to make changes to the iptables ruleset. | DOCK-1-3 |
| 2.5 | Ensure insecure registries are not used | Docker considers a private registry either secure or insecure. By default, registries are considered secure. | - |
| 2.6 | Ensure aufs storage driver is not used | Do not use aufs as the storage driver for your Docker instance. | DOCK-1-5 |
| 2.7 | Ensure TLS authentication for Docker daemon is configured | It is possible to make the Docker daemon available remotely over a TCP port. If this is required, you should ensure that TLS authentication is configured in order to restrict access to the Docker daemon via IP address and port. | DOCK-3-5; DOCK-3-6 |
| 2.8 | Ensure the default ulimit is configured appropriately | Set the default ulimit options as appropriate in your environment. | DOCK-2-3 |
| 2.9 | Enable user namespace support | You should enable user namespace support in Docker daemon to utilize container user to host user re-mapping. This recommendation is beneficial where the containers you are using do not have an explicit container user defined in the container image. If the container images that you are using have a pre-defined non-root user, this recommendation may be skipped as this feature is still in its infancy, and might result in unpredictable issues or difficulty in configuration. | - |
| 2.10 | Ensure the default cgroup usage has been confirmed | The --cgroup-parent option allows you to set the default cgroup parent to use for all containers. If there is no specific usage requirement for this, the setting should be left at its default. | DOCK-1-6; DOCK-4-5 |
| 2.11 | Ensure base device size is not changed until needed | Under certain circumstances, you might need containers larger than 10G. Where this applies you should carefully choose the base device size. | - |
| 2.12 | Ensure that authorization for Docker client commands is enabled | You should use native Docker authorization plugins or a third party authorization mechanism with the Docker daemon to manage access to Docker client commands. | ORCH-2-10; DOCK-4-3 |
| 2.13 | Ensure centralized and remote logging is configured | Docker supports various logging mechanisms. A preferable method for storing logs is one that supports centralized and remote management. | ORCH-3-5; DOCK-3-1; DOCK-3-4 |
| 2.14 | Ensure containers are restricted from acquiring new privileges | By default you should restrict containers from acquiring additional privileges via suid or sgid. | - |
| 2.15 | Ensure live restore is enabled | The --live-restore option enables full support of daemon-less containers within Docker. It ensures that Docker does not stop containers on shutdown or restore and that it properly reconnects to the container when restarted. | DOCK-1-7 |
| 2.16 | Ensure Userland Proxy is Disabled | The Docker daemon starts a userland proxy service for port forwarding whenever a port is exposed. Where hairpin NAT is available, this service is generally superfluous to requirements and can be disabled. | DOCK-2-4 |
| 2.17 | Ensure that a daemon-wide custom seccomp profile is applied if appropriate | You can choose to apply a custom seccomp profile at a daemon-wide level if needed with this overriding Docker's default seccomp profile. | MAN-4-2; CONT-3-5 |
| 2.18 | Ensure that experimental features are not implemented in production | Experimental features should not be enabled in production. | DOCK-1-4 |
| Docker Daemon Configuration Files |  |  |  |
| 3.1 | Ensure that the docker.service file ownership is set to root:root | You should verify that the docker.service file ownership and group ownership are correctly set to root. | ORCH-2-5; ORCH-2-6; DOCK-2-5 |
| 3.2 | Ensure that docker.service file permissions are appropriately set | You should verify that the docker.service file permissions are either set to 644 or to a more restrictive value. | ORCH-2-5; ORCH-2-6; DOCK-2-5 |
| 3.3 | Ensure that docker.socket file ownership is set to root:root | You should verify that the docker.socket file ownership and group ownership are correctly set to root. | ORCH-2-5; ORCH-2-6; DOCK-2-5 |
| 3.4 | Ensure that docker.socket file permissions are set to 644 or more restrictive | You should verify that the file permissions on the docker.socket file are correctly set to 644 or more restrictively. | ORCH-2-5; ORCH-2-6; DOCK-2-5 |
| 3.5 | Ensure that the /etc/docker directory ownership is set to root:root | You should verify that the /etc/docker directory ownership and group ownership is correctly set to root. | ORCH-2-5; ORCH-2-6; DOCK-2-5 |
| 3.6 | Ensure that /etc/docker directory permissions are set to 755 or more restrictively | You should verify that the /etc/docker directory permissions are correctly set to 755 or more restrictively. | ORCH-2-5; ORCH-2-6; DOCK-2-5 |
| 3.7 | Ensure that registry certificate file ownership is set to root:root | You should verify that all the registry certificate files (usually found under /etc/docker/certs.d/<registry-name> directory) are individually owned and group owned by root. | ORCH-2-5; ORCH-2-6; DOCK-2-5 |
| 3.8 | Ensure that registry certificate file permissions are set to 444 or more restrictively | You should verify that all the registry certificate files (usually found under /etc/docker/certs.d/<registry-name> directory) have permissions of 444 or are set more restrictively. | ORCH-2-5; ORCH-2-6; DOCK-2-5 |
| 3.9 | Ensure that TLS CA certificate file ownership is set to root:root | You should verify that the TLS CA certificate file (the file that is passed along with the --tlscacert parameter) is individually owned and group owned by root. | ORCH-2-5; ORCH-2-6; DOCK-2-5 |
| 3.10 | Ensure that TLS CA certificate file permissions are set to 444 or more restrictively | You should verify that the TLS CA certificate file (the file that is passed along with the --tlscacert parameter) has permissions of 444 or is set more restrictively. | ORCH-2-5; ORCH-2-6; DOCK-2-5 |
| 3.11 | Ensure that Docker server certificate file ownership is set to root:root | You should verify that the Docker server certificate file (the file that is passed along with the --tlscert parameter) is individual owned and group owned by root. | ORCH-2-5; ORCH-2-6; DOCK-2-5 |
| 3.12 | Ensure that the Docker server certificate file permissions are set to 444 or more restrictively | You should verify that the Docker server certificate file (the file that is passed along with the --tlscert parameter) has permissions of 444 or more restrictive permissions. | ORCH-2-5; ORCH-2-6; DOCK-2-5 |
| 3.13 | Ensure that the Docker server certificate key file ownership is set to root:root | You should verify that the Docker server certificate key file (the file that is passed along with the --tlskey parameter) is individually owned and group owned by root. | ORCH-2-5; ORCH-2-6; DOCK-2-5 |
| 3.14 | Ensure that the Docker server certificate key file permissions are set to 400 | You should verify that the Docker server certificate key file (the file that is passed along with the --tlskey parameter) has permissions of 400. | ORCH-2-5; ORCH-2-6; DOCK-2-5 |
| 3.15 | Ensure that the Docker socket file ownership is set to root:docker | You should verify that the Docker socket file is owned by root and group owned by docker. | ORCH-2-5; ORCH-2-6; DOCK-2-5 |
| 3.16 | Ensure that the Docker socket file permissions are set to 660 or more restrictively | You should verify that the Docker socket file has permissions of 660 or are configured more restrictively. | ORCH-2-5; ORCH-2-6; DOCK-2-5 |
| 3.17 | Ensure that the daemon.json file ownership is set to root:root | You should verify that the daemon.json file individual ownership and group ownership is correctly set to root, if it is in use. | ORCH-2-5; ORCH-2-6; DOCK-2-5 |
| 3.18 | Ensure that daemon.json file permissions are set to 644 or more restrictive | You should verify that if the daemon.json is present its file permissions are correctly set to 644 or more restrictively. | ORCH-2-5; ORCH-2-6; DOCK-2-5 |
| 3.19 | Ensure that the /etc/default/docker file ownership is set to root:root | You should verify that the /etc/default/docker file ownership and group-ownership is correctly set to root. | ORCH-2-5; ORCH-2-6; DOCK-2-5 |
| 3.20 | Ensure that the /etc/default/docker file permissions are set to 644 or more restrictively | You should verify that the /etc/default/docker file permissions are correctly set to 644 or more restrictively. | ORCH-2-5; ORCH-2-6; DOCK-2-5 |
| 3.21 | Ensure that the /etc/sysconfig/docker file permissions are set to 644 or more restrictively | You should verify that the /etc/sysconfig/docker file permissions are correctly set to 644 or more restrictively. | ORCH-2-5; ORCH-2-6; DOCK-2-5 |
| 3.22 | Ensure that the /etc/sysconfig/docker file ownership is set to root:root | You should verify that the /etc/sysconfig/docker file individual ownership and group ownership is correctly set to root. | ORCH-2-5; ORCH-2-6; DOCK-2-5 |
| 3.23 | Ensure that the Containerd socket file ownership is set to root:root | You should verify that the Containerd socket file is owned by root and group owned by root. | ORCH-2-5; ORCH-2-6; DOCK-2-5 |
| 3.24 | Ensure that the Containerd socket file permissions are set to 660 or more restrictively | You should verify that the Containerd socket file has permissions of 660 or are configured more restrictively. | ORCH-2-5; ORCH-2-6; DOCK-2-5 |
| Container Images and Build File Configuration |  |  |  |
| 4.1 | Ensure that a user for the container has been created | Containers should run as a non-root user. | MAN-3-2 |
| 4.2 | Ensure that containers use only trusted base images | You should ensure that container images you use are either written from scratch or are based on another established and trusted base image downloaded over a secure channel. | IMG-1-2 |
| 4.3 | Ensure that unnecessary packages are not installed in the container | Containers should have as small a footprint as possible, and should not contain unnecessary software packages which could increase their attack surface. | IMG-1-4; IMG-4-1 |
| 4.4 | Ensure images are scanned and rebuilt to include security patches | Images should be scanned frequently for any vulnerabilities. You should rebuild all images to include these patches and then instantiate new containers from them. | IMG-1-3; IMG-1-5; IMG-2-1; IMG-2-2; IMG-3-2; IMG-3-4 |
| 4.5 | Ensure Content trust for Docker is Enabled | Content trust is disabled by default and should be enabled in line with organizational security policy. | IMG-3-3 |
| 4.6 | Ensure that HEALTHCHECK instructions have been added to container images | You should add the HEALTHCHECK instruction to your Docker container images in order to ensure that health checks are executed against running containers. | MAN-2-1 |
| 4.7 | Ensure update instructions are not used alone in Dockerfiles | You should not use OS package manager update instructions such as apt-get updateor yum update either alone or in a single line in any Dockerfiles used to generate images under review. | IMG-1-4; IMG-3-2 |
| 4.8 | Ensure setuid and setgid permissions are removed | Removing setuid and setgid permissions in the images can prevent privilege escalation attacks within containers. | IMG-4-1 |
| 4.9 | Ensure that COPY is used instead of ADD in Dockerfiles | You should use the COPY instruction instead of the ADD instruction in the Dockerfile. | IMG-1-4 |
| 4.10 | Ensure secrets are not stored in Dockerfiles | Do not store any secrets in Dockerfiles. | IMG-1-1; MAN-1-1 |
| 4.11 | Ensure only verified packages are installed | You should verify the authenticity of packages before installing them into images. | IMG-1-4; IMG-2-1 |
| 4.12 | Ensure all signed artifacts are validated | Validate artifacts signatures before uploading to the package registry. | IMG-3-3 |
| Container Runtime Configuration |  |  |  |
| 5.1 | Ensure swarm mode is not Enabled, if not needed | Do not enable swarm mode on a Docker engine instance unless this is needed. | - |
| 5.2 | Ensure that, if applicable, an AppArmor Profile is enabled | AppArmor is an effective and easy-to-use Linux application security system. It is available on some Linux distributions by default, for example, on Debian and Ubuntu. | MAN-4-1 |
| 5.3 | Ensure that, if applicable, SELinux security options are set | SELinux is an effective and easy-to-use Linux application security system. It is available by default on some distributions such as Red Hat and Fedora. | - |
| 5.4 | Ensure that Linux kernel capabilities are restricted within containers | By default, Docker starts containers with a restricted set of Linux kernel capabilities. This means that any process can be granted the required capabilities instead of giving it root access. Using Linux kernel capabilities, processes in general do not need to run as the root user. | MAN-3-4 |
| 5.5 | Ensure that privileged containers are not used | Using the --privileged flag provides all Linux kernel capabilities to the container to which it is applied and therefore overwrites the --cap-add and --cap-drop flags. For this reason you should ensure that it is not used. | MAN-1-2; MAN-1-4; MAN-3-2 |
| 5.6 | Ensure sensitive host system directories are not mounted on containers | You should not allow sensitive host system directories such as those listed below to be mounted as container volumes, especially in read-write mode. / /boot /dev /etc /lib /lib64 /proc /sys /usr | MAN-2-2; MAN-2-4 |
| 5.7 | Ensure sshd is not run within containers | The SSH daemon should not be running within the container. You should SSH into the Docker host, and use docker exec to enter a container. | CONT-1-3 |
| 5.8 | Ensure privileged ports are not mapped within containers | The TCP/IP port numbers below 1024 are considered privileged ports. Normal users and processes are not allowed to use them for various security reasons. Docker does, however allow a container port to be mapped to a privileged port. | - |
| 5.9 | Ensure that only needed ports are open on the container | The dockerfile for a container image defines the ports which are opened by default on a container instance. The list of ports are relevant to the application you are running within the container and should only be open if they are needed. | - |
| 5.10 | Ensure that the host's network namespace is not shared | When the networking mode on a container is set to --net=host, the container is not placed inside a separate network stack. Effectively, applying this option instructs Docker to not containerize the container's networking. The consequence of this is that the container lives "outside" in the main Docker host and has full access to its network interfaces. | DOCK-4-4; MAN-1-3 |
| 5.11 | Ensure that the memory usage for containers is limited | By default, all containers on a Docker host share resources equally. By using the resource management capabilities of the Docker host, you can control the amount of memory that a container is able to use. | MAN-2-8; CONT-1-2 |
| 5.12 | Ensure that CPU priority is set appropriately on containers | By default, all containers on a Docker host share resources equally. By using the resource management capabilities of the Docker host you can control the host CPU resources that a container may consume. | MAN-2-8; CONT-1-2 |
| 5.13 | Ensure that the container's root filesystem is mounted as read only | The container's root filesystem should be treated as a 'golden image' by using Docker run's --read-only option. This prevents any writes to the container's root filesystem at container runtime and enforces the principle of immutable infrastructure. | MAN-2-2; MAN-3-3 |
| 5.14 | Ensure that incoming container traffic is bound to a specific host interface | By default, Docker containers can make connections to the outside world, but the outside world cannot connect to containers and each outgoing connection will appear to originate from one of the host machine's own IP addresses. You should only allow container services to be contacted through a specific external interface on the host machine. | - |
| 5.15 | Ensure that the 'on-failure' container restart policy is set to '5' | By using the --restart flag in the docker run command you can specify a restart policy for how a container should or should not be restarted on exit. You should choose the on-failure restart policy and limit the restart attempts to 5. | - |
| 5.16 | Ensure that the host's process namespace is not shared | The Process ID (PID) namespace isolates the process ID space, meaning that processes in different PID namespaces can have the same PID. This creates process level isolation between the containers and the host. | MAN-2-5 |
| 5.17 | Ensure that the host's IPC namespace is not shared | IPC (POSIX/SysV IPC) namespace provides separation of named shared memory segments, semaphores and message queues. The IPC namespace on the host should therefore not be shared with containers and should remain isolated. | MAN-2-6 |
| 5.18 | Ensure that host devices are not directly exposed to containers | Host devices can be directly exposed to containers at runtime. Do not directly expose host devices to containers, especially to containers that are not trusted. | - |
| 5.19 | Ensure that the default ulimit is overwritten at runtime if needed | The default ulimit is set at the Docker daemon level. However, if you need to, you may override the default ulimit setting during container runtime. | MAN-2-8; CONT-1-2 |
| 5.20 | Ensure mount propagation mode is not set to shared | Mount propagation mode allows mounting volumes in shared, slave or private mode on a container. Do not use shared mount propagation mode unless explicitly needed. | MAN-2-2 |
| 5.21 | Ensure that the host's UTS namespace is not shared | UTS namespaces provide isolation between two system identifiers: the hostname and the NIS domain name. It is used to set the hostname and the domain which are visible to running processes in that namespace. Processes running within containers do not typically require to know either the hostname or the domain name. The UTS namespace should therefore not be shared with the host. | MAN-2-7 |
| 5.22 | Ensure the default seccomp profile is not Disabled | Seccomp filtering provides a means for a process to specify a filter for incoming system calls. The default Docker seccomp profile works on a whitelist basis and allows for a large number of common system calls, whilst blocking all others. This filtering should not be disabled unless it causes a problem with your container application usage. | MAN-4-2; CONT-1-1 |
| 5.23 | Ensure that docker exec commands are not used with the privileged option | You should not use docker exec with the --privileged option. | - |
| 5.24 | Ensure that docker exec commands are not used with the user=root option | You should not use docker exec with the --user=root option. | MAN-2-4 |
| 5.25 | Ensure that cgroup usage is confirmed | It is possible to attach to a particular cgroup when a container is instantiated. Confirming cgroup usage would ensure that containers are running in defined cgroups. | DOCK-4-5 |
| 5.26 | Ensure that the container is restricted from acquiring additional privileges | You should restrict the container from acquiring additional privileges via suid or sgid bits. | MAN-1-4; MAN-3-2 |
| 5.27 | Ensure that container health is checked at runtime | If the container image does not have an HEALTHCHECK instruction defined, you should use the --health-cmd parameter at container runtime to check container health. | ORCH-3-5; DOCK-3-4; MAN-2-1 |
| 5.28 | Ensure that Docker commands always make use of the latest version of their image | You should always ensure that you are using the latest version of the images within your repository and not cached older versions. | MAN-2-3 |
| 5.29 | Ensure that the PIDs cgroup limit is used | You should use the --pids-limit flag at container runtime. | MAN-2-8; CONT-1-2 |
| 5.30 | Ensure that Docker's default bridge "docker0" is not used | You should not use Docker's default bridge docker0. Instead you should use Docker's user-defined networks for container networking. | DOCK-4-4 |
| 5.31 | Ensure that the host's user namespaces are not shared | You should not share the host's user namespaces with containers running on it. | - |
| 5.32 | Ensure that the Docker socket is not mounted inside any containers | The Docker socket docker.sock should not be mounted inside a container. | - |
| Docker Security Operations |  |  |  |
| 6.1 | Ensure that image sprawl is avoided | You should not keep a large number of container images on the same host. Use only tagged images as appropriate. | DOCK-2-8 |
| 6.2 | Ensure that container sprawl is avoided | You should not keep a large number of containers on the same host. | ORCH-3-5; DOCK-3-4 |
| Docker Swarm Configuration |  |  |  |
| 7.1 | Ensure that the minimum number of manager nodes have been created in a swarm | You should ensure that the minimum number of required manager nodes is created in a swarm. | DOCK-1-8 |
| 7.2 | Ensure that swarm services are bound to a specific host interface | By default, Docker swarm services will listen on all interfaces on the host. This may not be necessary for the operation of the swarm where the host has multiple network interfaces. | DOCK-2-13 |
| 7.3 | Ensure that all Docker swarm overlay networks are encrypted | Ensure that all Docker swarm overlay networks are encrypted. | ORCH-1-5; DOCK-2-14 |
| 7.4 | Ensure that Docker's secret management commands are used for managing secrets in a swarm cluster | You should use Docker's in-built secret management command for control of secrets. | ORCH-3-6; DOCK-3-8 |
| 7.5 | Ensure that swarm manager is run in auto-lock mode | You should review whether you wish to run Docker swarm manager in auto-lock mode. | DOCK-4-6 |
| 7.6 | Ensure that the swarm manager auto-lock key is rotated periodically | You should rotate the swarm manager auto-lock key periodically. | DOCK-4-7 |
| 7.7 | Ensure that node certificates are rotated as appropriate | You should rotate swarm node certificates in line with your organizational security policy. | DOCK-2-15 |
| 7.8 | Ensure that CA certificates are rotated as appropriate | You should rotate root CA certificates as appropriate. | DOCK-3-9 |
| 7.9 | Ensure that management plane traffic is separated from data plane traffic | You should separate management plane traffic from data plane traffic. | DOCK-2-12 |
